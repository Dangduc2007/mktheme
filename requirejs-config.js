var config = {
    paths: {
        slick:        'js/slick',
        owlCarousel:        'js/owl.carousel'
    },
    shim: {
        slick: {
            deps: ['jquery']
        },
        owlCarousel: {
            deps: ['jquery']
        }
    }
    
};
